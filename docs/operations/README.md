# Operations

Operational workflow notes for updating, deploying, and checking Liahona.AI.

Keep this folder command-oriented and safe to follow.

Start with `commands.md` for the minimal Windows PowerShell operations layer.
