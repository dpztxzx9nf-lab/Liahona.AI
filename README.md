# Liahona.AI

Artifact-first GitHub Pages interface and continuity map for Liahona.

Project documentation lives in `docs/`.

Repository working guidance for Codex lives in `AGENTS.md`.

Minimal Windows operations live in `ops\`:

```powershell
.\ops\status.ps1
.\ops\check.ps1
.\ops\serve.ps1
```
